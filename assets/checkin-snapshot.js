(function () {
  function escape(value) { return String(value ?? '').replace(/[&<>"']/g, c => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c])); }
  function render(snapshot, submittedAt, reviewStatus = 'pending') {
    const domainList = ids => `<ul>${ids.map(id => `<li>${escape(snapshot.domains.find(d => d.id === id)?.name)}</li>`).join('')}</ul>`;
    return `<article class="ci-snapshot">
      <header class="ci-snapshot-head"><p class="ci-kicker">Music Leader Sustainability Check-In™</p><h1 class="ci-title" tabindex="-1">Your Sustainability<br><em>Snapshot.</em></h1><p class="ci-lead">A moment to notice what’s supporting you, where you may be carrying pressure, and what deserves a closer look.</p><p class="ci-snapshot-meta">Completed ${escape(new Date(submittedAt).toLocaleDateString(undefined, { year: 'numeric', month: 'long', day: 'numeric' }))} · Pre-pilot reflection</p></header>
      <div class="ci-notice"><p>This Snapshot is based on your responses and is intended as a starting point for reflection and conversation. It is not a diagnosis, grade, or evaluation of your professional ability.</p><p>As part of this pre-pilot, Ashley will also review your Snapshot alongside your feedback.</p></div>
      <div class="ci-atglance"><section class="ci-card"><p class="ci-kicker">What’s working</p><h2>What’s supporting you</h2>${snapshot.supporting.length ? domainList(snapshot.supporting) : '<p>Your answers don’t identify a Strong or Stable area right now. That doesn’t mean you have no strengths. Ashley can help you explore support that these statements may not capture.</p>'}<p>Notice what deserves protection, not just what needs to change.</p></section><section class="ci-card"><p class="ci-kicker">What’s weighing</p><h2>Where pressure may be showing up</h2>${snapshot.pressure.length ? domainList(snapshot.pressure) : '<p>All eight areas currently fall within Strong or Stable. There is no need to manufacture a problem. Consider what helps these conditions remain sustainable.</p>'}<p>${snapshot.domains.filter(d => d.average < 3.5).length > 3 ? 'Several areas may deserve attention. These are a few starting points for your conversation, not a list of things you must fix.' : 'An area of pressure is not automatically your personal responsibility to fix.'}</p></section></div>
      <h2 class="ci-section-title">Eight areas. Your experience.</h2><p class="ci-small">Open each area for its meaning, a reflection prompt, and a possible support pathway.</p>
      <div>${snapshot.domains.map((d, i) => `<details class="ci-domain-result"><summary><span class="ci-result-number">${String(i + 1).padStart(2, '0')}</span><span class="ci-result-name">${escape(d.name)}<span class="ci-signal">${escape(d.signal)}</span></span></summary><div class="ci-result-copy"><p><strong>${escape(d.shortMeaning)}</strong></p><p>${escape(d.meaning)}</p><p class="ci-reflection"><strong>A question to sit with</strong>${escape(d.reflection)}</p><p><strong>Possible support pathway</strong><br>${escape(d.pathway)}</p></div></details>`).join('')}</div>
      <section class="ci-outlook"><p class="ci-kicker">Looking ahead · Separate from your eight areas</p><h2>Your three-year outlook</h2><p>${escape(snapshot.outlook.question)}</p><strong>${escape(snapshot.outlook.value === 1 ? 'Significant sustainability concern' : snapshot.outlook.label)}</strong><p>${escape(snapshot.outlook.reflection)}</p></section>
      <section><h2 class="ci-section-title">Return to what brought you here.</h2>${snapshot.heaviest ? `<p class="ci-small">When you began, this is what felt heaviest—in your own words:</p><blockquote class="ci-quote">${escape(snapshot.heaviest)}</blockquote>` : '<p>You chose not to add a written reflection at the beginning. You can still consider what feels heaviest now.</p>'}<p>Looking at this Snapshot, what might actually be underneath it?</p>${snapshot.hope ? `<p class="ci-small">What you hoped might become different:</p><blockquote class="ci-quote">${escape(snapshot.hope)}</blockquote>` : ''}</section>
      <div class="ci-notice">For now, you don’t need to fix anything. Bring this Snapshot to your conversation with Ashley. Together, you can consider what deserves attention and what may be within reach.</div>
    </article>`;
  }
  window.CheckinSnapshot = { render, escape };
})();
